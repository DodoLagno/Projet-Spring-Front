// src/components/ActeurPagination.js
import React from 'react';

const ActeurPagination = ({ onPageChange, currentPage, totalPages }) => {
    // Implement pagination controls here
    return (
        <div>
            {/* Display pagination controls */}
        </div>
    );
};

export default ActeurPagination;